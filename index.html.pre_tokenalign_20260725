<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>너울(NEOUL) — 교육자료 배포 시스템 | 무료 학습자료</title>
<meta name="description" content="너울 수능·내신 무료 학습자료 배포. 국어·수학·영어·사탐·과탐·한국사·통합사회·통합과학. SKY 출신 교과 멘토와 AI가 협업해 출제한 자료. 상표 출원 40-2026-0081306·0081307." />
<meta name="theme-color" content="#0A3D62" />
<meta property="og:title" content="너울 — 교육자료 배포 시스템" />
<meta property="og:description" content="오늘의 한 물결이, 내일의 너울이 된다. 수능·내신 무료 학습자료 원클릭 다운로드." />
<link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Cpath d='M2 13c4 0 4-6 8-6s4 6 8 6 4-6 8-6' stroke='%230A3D62' stroke-width='2.6' fill='none' stroke-linecap='round'/%3E%3Cpath d='M2 21c4 0 4-5 8-5s4 5 8 5 4-5 8-5' stroke='%233498DB' stroke-width='2.4' fill='none' stroke-linecap='round'/%3E%3C/svg%3E" />
<link rel="preconnect" href="https://fonts.googleapis.com" /><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@600;700&display=swap" rel="stylesheet" />
<link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css" />
<style>
  :root{--deep:#0A3D62;--ocean:#1B6CA8;--sky:#3498DB;--wave:#AED6F1;--mist:#EAF4FB;--line:#D6E9F5;--ink:#12222f;--muted:#5A7890;--serif:"Noto Serif KR",serif;--sans:"Pretendard","Noto Sans KR",system-ui,sans-serif}
  *{box-sizing:border-box;margin:0;padding:0}
  body{font-family:var(--sans);color:var(--ink);background:#fff;line-height:1.6;-webkit-font-smoothing:antialiased}
  .wrap{max-width:1000px;margin:0 auto;padding:0 20px}
  a{color:inherit;text-decoration:none}
  .hero{background:linear-gradient(155deg,#0A3D62,#1B6CA8);color:#fff;padding:48px 0 40px;text-align:center}
  .hero .logo{display:inline-flex;align-items:center;gap:9px;font-family:var(--serif);font-weight:700;font-size:1.5rem}
  .hero .logo svg{width:34px;height:34px}
  .hero h1{font-family:var(--serif);font-size:1.4rem;margin:14px 0 6px;font-weight:700}
  .hero p{color:#d9ecf7;font-size:.94rem}
  .hero .tag{font-family:var(--serif);color:var(--wave);font-size:.9rem;margin-top:12px}
  .notice{background:var(--mist);border-bottom:1px solid var(--line)}
  .notice .wrap{padding:14px 20px;font-size:.86rem;color:var(--muted)}
  .notice b{color:var(--deep)}
  .tabs{position:sticky;top:0;z-index:10;background:rgba(255,255,255,.94);backdrop-filter:blur(10px);border-bottom:1px solid var(--line)}
  .tabs .wrap{display:flex;gap:7px;overflow-x:auto;padding:12px 20px}
  .tab{white-space:nowrap;border:1.5px solid var(--line);background:#fff;color:var(--muted);border-radius:20px;padding:7px 15px;font-weight:700;font-size:.86rem;cursor:pointer;font-family:inherit}
  .tab.on{background:var(--deep);color:#fff;border-color:var(--deep)}
  main{padding:28px 0 60px;min-height:40vh}
  .state{text-align:center;color:var(--muted);padding:60px 20px}
  .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:16px}
  .card{border:1px solid var(--line);border-radius:14px;padding:20px;background:#fff;transition:.2s}
  .card:hover{box-shadow:0 14px 34px -20px rgba(10,61,98,.4);transform:translateY(-2px)}
  .card .cat{font-size:.74rem;font-weight:800;color:var(--ocean);letter-spacing:.04em}
  .card h3{font-family:var(--serif);font-size:1.12rem;color:var(--deep);margin:5px 0 4px}
  .badge{display:inline-block;font-size:.7rem;font-weight:700;padding:3px 9px;border-radius:12px;margin-bottom:10px}
  .badge.author{background:#eaf4fb;color:#1B6CA8}
  .badge.ai{background:#fff4e5;color:#9a5b00}
  .files{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}
  .dl{display:inline-flex;align-items:center;gap:6px;background:var(--sky);color:#fff;border-radius:9px;padding:9px 14px;font-weight:700;font-size:.85rem;cursor:pointer;border:none;font-family:inherit}
  .dl:hover{background:#2980B9}
  .dl.ghost{background:#fff;color:var(--deep);border:1.5px solid var(--line)}
  footer{background:#06283d;color:#93aabd;padding:34px 0;font-size:.82rem}
  footer .wrap{text-align:center}footer b{color:#fff}
  footer .cr{margin-top:8px;font-size:.76rem;color:#6d8ba3}
  /* ── 게이티드 퍼널 모달 ── */
  .modal-bg{position:fixed;inset:0;background:rgba(6,40,61,.55);backdrop-filter:blur(3px);display:none;align-items:center;justify-content:center;z-index:100;padding:18px}
  .modal-bg.on{display:flex}
  .modal{background:#fff;border-radius:18px;max-width:420px;width:100%;padding:28px 26px 24px;box-shadow:0 30px 70px -20px rgba(6,40,61,.6);animation:pop .18s ease}
  @keyframes pop{from{transform:translateY(10px) scale(.98);opacity:0}to{transform:none;opacity:1}}
  .modal h2{font-family:var(--serif);color:var(--deep);font-size:1.24rem;margin-bottom:6px}
  .modal .sub{color:var(--muted);font-size:.88rem;margin-bottom:18px}
  .modal label{display:block;font-size:.8rem;font-weight:700;color:var(--deep);margin:12px 0 5px}
  .modal input[type=email],.modal select{width:100%;border:1.5px solid var(--line);border-radius:10px;padding:11px 12px;font-size:.92rem;font-family:inherit;color:var(--ink);background:#fff}
  .modal input[type=email]:focus,.modal select:focus{outline:none;border-color:var(--sky)}
  .modal .agree{display:flex;align-items:flex-start;gap:8px;margin:16px 0 4px;font-size:.8rem;color:var(--muted);line-height:1.45}
  .modal .agree input{margin-top:3px}
  .modal .submit{width:100%;margin-top:16px;background:linear-gradient(135deg,#0A3D62,#2B5F8B);color:#fff;border:none;border-radius:11px;padding:13px;font-weight:800;font-size:.98rem;cursor:pointer;font-family:inherit}
  .modal .submit:hover{filter:brightness(1.08)}
  .modal .fine{font-size:.72rem;color:#93a7b8;margin-top:12px;text-align:center;line-height:1.5}
  .modal .x{float:right;border:none;background:none;font-size:1.3rem;color:var(--muted);cursor:pointer;line-height:1;margin:-6px -4px 0 0}
  .modal .err{color:#c0392b;font-size:.78rem;margin-top:8px;min-height:1em}
</style>
</head>
<body>
<header class="hero">
  <div class="wrap">
    <span class="logo"><svg viewBox="0 0 32 32"><path d="M2 13c4 0 4-6 8-6s4 6 8 6 4-6 8-6" stroke="#fff" stroke-width="2.6" fill="none" stroke-linecap="round"/><path d="M2 21c4 0 4-5 8-5s4 5 8 5 4-5 8-5" stroke="#AED6F1" stroke-width="2.4" fill="none" stroke-linecap="round"/></svg>너울 NEOUL</span>
    <h1>교육자료 배포 시스템</h1>
    <p>수능·내신 무료 학습자료 · 원클릭 다운로드</p>
    <p class="tag">"오늘의 한 물결이, 내일의 너울이 된다."</p>
  </div>
</header>

<div class="notice"><div class="wrap">
  📚 <b>자료 출처 안내</b> — 본 자료는 <b>SKY 출신 교과 멘토와 AI의 협업</b>으로 출제·정리한 너울의 자체 제작 자료입니다. 외부 기관의 강의·모의고사 자료를 무단으로 재가공하지 않습니다.
</div></div>

<nav class="tabs"><div class="wrap" id="tabs"></div></nav>

<main><div class="wrap">
  <div id="state" class="state">너울 학습자료를 불러오는 중입니다…<br/><span style="font-size:.82rem">최초 접속 시 5~10초 정도 소요될 수 있습니다.</span></div>
  <div id="grid" class="grid" style="display:none"></div>
</div></main>

<footer><div class="wrap">
  <b>너울 NEOUL</b> · 교육자료 배포 시스템<br/>
  상표 출원번호 40-2026-0081306 · 40-2026-0081307
  <div class="cr">© 2026 NEOUL. All rights reserved. · 자료는 학습 목적 제공이며 무단 상업적 재배포를 금합니다.</div>
</div></footer>

<!-- 게이티드 퍼널 모달 -->
<div class="modal-bg" id="modalBg">
  <div class="modal" role="dialog" aria-modal="true">
    <button class="x" id="modalClose" aria-label="닫기">×</button>
    <h2>무료 학습자료 받기</h2>
    <p class="sub">이 자료는 <b>무료</b>입니다. 이메일과 간단한 2가지만 알려주시면 바로 받으실 수 있어요.</p>
    <label for="fEmail">이메일</label>
    <input type="email" id="fEmail" placeholder="you@example.com" autocomplete="email" />
    <label for="fGrade">학년</label>
    <select id="fGrade">
      <option value="">선택해 주세요</option>
      <option>고1</option><option>고2</option><option>고3</option><option>N수생</option><option>학부모·기타</option>
    </select>
    <label for="fInterest">관심 영역</label>
    <select id="fInterest">
      <option value="">선택해 주세요</option>
      <option>국어</option><option>수학</option><option>영어</option><option>사탐</option><option>과탐</option><option>한국사</option><option>통합사회</option><option>통합과학</option>
    </select>
    <label class="agree"><input type="checkbox" id="fAgree" /><span>개인정보(이메일·학년·관심영역) 수집·이용에 동의합니다. 수집 정보는 <b>자료 제공·학습 안내 목적</b>으로만 사용되며 제3자에게 제공하지 않습니다.</span></label>
    <div class="err" id="fErr"></div>
    <button class="submit" id="fSubmit">자료 받기 →</button>
    <div class="fine">신청 즉시 자료가 새 창으로 열립니다. 언제든 수신거부하실 수 있습니다.</div>
  </div>
</div>

<script>
  // tmkim Apps Script 웹앱 URL(/exec). 백엔드 v2 재배포 후에도 URL이 바뀌면 여기만 교체.
  var APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbzsVNBCcCwLMavq8gcgYAx8p3ss5Xj9vue3vRmqeUziGIqnwySjZ_uK5gk-hlkrfKGl/exec";
  var LEAD_KEY = "neoul_lead_v1";
  var CATS = ["전체","국어","수학","영어","사탐","과탐","한국사","통합사회","통합과학"];
  var ALL = [], cur = "전체", pending = null;

  function esc(s){ return String(s).replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }
  function getLead(){ try{ return JSON.parse(localStorage.getItem(LEAD_KEY)||'null'); }catch(e){ return null; } }
  function saveLead(o){ try{ localStorage.setItem(LEAD_KEY, JSON.stringify(o)); }catch(e){} }

  // 리드/다운로드 로그 전송 (fire-and-forget, PII는 본문에만)
  function logLead(email, grade, interest, material){
    try{
      var body = 'action=lead'
        + '&email=' + encodeURIComponent(email||'')
        + '&grade=' + encodeURIComponent(grade||'')
        + '&interest=' + encodeURIComponent(interest||'')
        + '&material=' + encodeURIComponent(material||'')
        + '&consent=Y'
        + '&ua=' + encodeURIComponent((navigator.userAgent||'').slice(0,180));
      fetch(APPS_SCRIPT_URL, { method:'POST', mode:'no-cors', credentials:'omit',
        headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'}, body: body });
    }catch(e){}
  }
  function startDownload(href){ if(href){ window.open(href, '_blank', 'noopener'); } }

  // 탭
  var tabsEl = document.getElementById('tabs');
  CATS.forEach(function(c){
    var b=document.createElement('button'); b.className='tab'+(c==='전체'?' on':''); b.textContent=c;
    b.onclick=function(){cur=c;[].forEach.call(tabsEl.children,function(x){x.classList.remove('on')});b.classList.add('on');render();};
    tabsEl.appendChild(b);
  });

  window.__neoulData = function(res){
    if(!res || !res.ok){ fail(res && res.error); return; }
    ALL = res.items || [];
    render();
  };
  function fail(msg){
    document.getElementById('state').innerHTML='⚠️ 자료를 불러오지 못했습니다.<br/><span style="font-size:.82rem">'+(msg||'잠시 후 새로고침해 주세요.')+'</span>';
  }
  function render(){
    var grid=document.getElementById('grid'), state=document.getElementById('state');
    var items = cur==='전체' ? ALL : ALL.filter(function(x){return x.category===cur;});
    var groups={};
    items.forEach(function(x){ var k=x.category+'|'+x.subject+'|'+x.provenance; (groups[k]=groups[k]||[]).push(x); });
    var keys=Object.keys(groups);
    if(keys.length===0){ state.style.display='block'; grid.style.display='none'; state.innerHTML='해당 과목의 자료가 아직 없습니다.'; return; }
    state.style.display='none'; grid.style.display='grid'; grid.innerHTML='';
    keys.forEach(function(k){
      var arr=groups[k], first=arr[0];
      var files=arr.map(function(f){
        var mat=first.category+'_'+first.subject+'_'+f.type;
        return '<button class="dl'+(f.type==='해설편'?' ghost':'')+'" data-href="'+esc(f.download)+'" data-mat="'+esc(mat)+'" data-cat="'+esc(first.category)+'">⬇ '+esc(f.type)+'</button>';
      }).join('');
      var card=document.createElement('div'); card.className='card';
      card.innerHTML='<div class="cat">'+esc(first.category)+'</div><h3>'+esc(first.subject)+'</h3>'+
        '<span class="badge author">'+esc(first.provenance)+'</span>'+
        '<div class="files">'+files+'</div>';
      grid.appendChild(card);
    });
  }

  // 다운로드 클릭 → 게이팅
  document.getElementById('grid').addEventListener('click', function(e){
    var b = e.target.closest('.dl'); if(!b) return;
    var href=b.getAttribute('data-href'), mat=b.getAttribute('data-mat'), cat=b.getAttribute('data-cat');
    var lead=getLead();
    if(lead && lead.done){ logLead(lead.email, lead.grade, lead.interest, mat); startDownload(href); return; }
    openModal(href, mat, cat);
  });

  // 모달
  var modalBg=document.getElementById('modalBg');
  function openModal(href, mat, cat){
    pending={href:href, mat:mat};
    document.getElementById('fErr').textContent='';
    var sel=document.getElementById('fInterest');
    if(cat && cat!=='전체'){ for(var i=0;i<sel.options.length;i++){ if(sel.options[i].value===cat){ sel.selectedIndex=i; break; } } }
    modalBg.classList.add('on');
    setTimeout(function(){ document.getElementById('fEmail').focus(); }, 60);
  }
  function closeModal(){ modalBg.classList.remove('on'); pending=null; }
  document.getElementById('modalClose').onclick=closeModal;
  modalBg.addEventListener('click', function(e){ if(e.target===modalBg) closeModal(); });

  document.getElementById('fSubmit').onclick=function(){
    var email=document.getElementById('fEmail').value.trim();
    var grade=document.getElementById('fGrade').value;
    var interest=document.getElementById('fInterest').value;
    var agree=document.getElementById('fAgree').checked;
    var err=document.getElementById('fErr');
    if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)){ err.textContent='올바른 이메일을 입력해 주세요.'; return; }
    if(!agree){ err.textContent='개인정보 수집·이용 동의가 필요합니다.'; return; }
    logLead(email, grade, interest, pending?pending.mat:'');
    saveLead({email:email, grade:grade, interest:interest, done:1});
    var href = pending?pending.href:null;
    closeModal();
    startDownload(href);
  };

  (function load(){
    // 자료 목록 = Vercel 호스팅 매니페스트(same-origin). Drive/Apps Script 의존 제거.
    fetch('materials.json?t='+Date.now(),{cache:'no-store'})
      .then(function(r){ if(!r.ok) throw 0; return r.json(); })
      .then(function(res){
        ALL=(res&&res.items)||[];
        ALL.forEach(function(x){ x.download='materials/'+encodeURIComponent(x.file); if(!x.provenance) x.provenance=(res&&res.provenance)||'SKY 멘토 × AI 협업 출제'; });
        render();
      })
      .catch(function(){ fail('자료를 불러오지 못했습니다. 잠시 후 새로고침해 주세요.'); });
  })();
</script>
</body>
</html>
